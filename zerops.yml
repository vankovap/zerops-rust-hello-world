zerops:
  - setup: helloworld
    build:
      base: rust@1
      buildCommands:
        - cargo b --release
      deploy: target/release/~app
      envVariables:
        test: buildTestValue
    run:
      start: ./app
    envVariables: 
      test: testValue
