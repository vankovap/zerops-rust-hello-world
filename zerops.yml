zerops:
  - setup: helloworld
    build:
      base: rust@1
      buildCommands:
        - cargo b --release
      deploy: target/release/~app
      envVariables:
        buildTest: buildTestV
        buildTest1: build
    run:
      start: ./app
      envVariables: 
        runTest: runTest
        buildConfig: config
    envVariables: 
      test1: testValue1
      runTest1: runTest11
