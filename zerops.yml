zerops:
  - setup: helloworld
    build:
      base: rust@1
      buildCommands:
        - cargo b --release
      deploy: target/release/~app
      envVariables:
        buildTest: buildTestValue
    run:
      start: ./app
      envVariables: 
        runTest: runTestValue
    envVariables: 
      test: testValue
