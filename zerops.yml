zerops:
  - setup: helloworld
    build:
      base: rust@1
      buildCommands:
        - cargo b --release
      deploy: target/release/~app
      envVariables:
        buildTest: buildTestV
        buildTest1: build
        buildConfig: config
    run:
      start: ./app
      envVariables: 
        runTest: runTest
    envVariables: 
      test1: testValue1
